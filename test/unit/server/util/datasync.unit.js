'use strict';

const chai = require('chai');
const chaiAsPromised = require('chai-as-promised');
chai.use(chaiAsPromised);
// const expect = chai.expect;
// const datasync = require('app/server/util/datasync.js');

describe('General Purpose Data Sync', () => {
  it('should fetch data with many values and one reference');
  it('should partition merged data for insert and update');
  it('should insert incoming merged data');
  it('should update incoming merged data');
});
