'use strict';

const sampleids = require('./sampleids.js');
const artifacts = require('./artifacts.js');
const labtests = require('./labtests.js');
const changes = require('./changes.js');

module.exports = {
  sampleids, artifacts, labtests, changes
};
