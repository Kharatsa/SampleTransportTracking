'use strict';

import React from 'react'; // eslint-disable-line no-unused-vars
import {Changes} from '../containers';

export const ChangesPage = (props) =>
  React.createElement(Changes, props);

export default ChangesPage;
